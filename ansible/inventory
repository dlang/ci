[container_hosts]
ci.dlang.io

# group with all containers
[containers:children]
ci_containers

[ci_containers]
jenkins.ci.lxd
runner-01.ci.lxd
runner-02.ci.lxd

[ci_containers:vars]
# ssh proxy into container (running ssh itself)
ansible_ssh_common_args=-o ProxyCommand="ssh -W %h:%p ci.dlang.io"
ansible_ssh_user=root

# grouped by purpose
[jenkins_slaves]
runner-01.ci.lxd
runner-02.ci.lxd

# all running at oneline.net atm
[online_net:children]
container_hosts
containers
